# GRID — Autonomous Trading Intelligence

Signal generation, strategy synthesis, and trade execution system.

## Structure
- `agents/` — 13 AI agents (knowledge, strategy, analysis layers)
- `api/` — REST API routes for trading operations
- `config/` — Agent prompts, risk limits, exchange config
